/*
** DESCRIPTION
**     La fonction atoi() convertit le début de la chaîne pointée
**     par nptr (Nomber of PoinTeRs) en entier de type int. Le 
**     résultat est identique à un appel 
**     strtol(nptr, (char **) NULL, 10); à la différence
**     que atoi() ne détecte pas d'erreur.
** 
** VALEUR RENVOYÉE
**     Le résultat de la conversion.
*/

#include "libft.h"

int     ft_atoi(const char *nptr)
{
    
}